<%= form_with(model: depoimento) do |form| %>
  <% if depoimento.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(depoimento.errors.count, "error") %> prohibited this depoimento from being saved:</h2>

      <ul>
        <% depoimento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_area :descricao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
